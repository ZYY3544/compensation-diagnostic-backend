"""
HAY 岗位价值评估引擎（package）

从 student-value-backend 摘出的核心 HAY 收敛引擎，剥掉了学生场景代码（5 维雷达、
学校系数、薪酬计算、趣味标签等）。这里只做**岗位价值评估**：

  输入：岗位 JD 文本 + 职能 + 岗位名
  输出：8 因子档位 + 三维分数 + 总分 + Hay 职级 (9-27)

只有 1 次 LLM 调用（提取 PK 因子档位），其余 7 步全是规则计算（查表 + 约束验证）。
"""
